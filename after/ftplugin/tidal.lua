local lspconfig = require("lspconfig")

-- require("lspconfig.configs").tidal_ls = {
-- 	default_config = {
-- 		cmd = { vim.fn.expand("~/.local/bin/tidal-ls") },
-- 		filetypes = { "tidal" },
-- 		root_dir = lspconfig.util.root_pattern(".root"),
-- 		settings = {},
-- 	},
-- }
--
-- lspconfig.tidal_ls.setup({})
